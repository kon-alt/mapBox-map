import React from 'react';
import {CustomMap} from "./components/customMap";
import './App.css'

const App = () => {
    return (
        <div className={'container'}>
            <CustomMap />
        </div>

    );
};

export default App;
